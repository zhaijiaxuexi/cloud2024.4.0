server:
  port: 2001

spring:
  application:
    name: seata-order-service # 在 Nacos 中显示的服务名

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/seata_order?characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # Nacos 的地址
    sentinel:
      transport:
        dashboard: localhost:8858 # Sentinel控制台地址
        port: 8719 # 默认8719，假如被占用会自动从8719+1依次扫描
#      # 配置持久化数据源
#      datasource:
#        # 名字随便起，代表流控规则
#        flow-rules:
#          nacos:
#            server-addr: localhost:8848
#            dataId: ${spring.application.name}-flow-rules # 建议：服务名-flow-rules
#            groupId: SENTINEL_GROUP
#            data-type: json
#            rule-type: flow # 代表这是流控规则



#    # --- 下面是你要配置的部分 ---
#    openfeign:
#      client:
#        config:
#          default: # 这里的 default 表示对所有服务生效
#            # 连接超时时间（毫秒）
#            connectTimeout: 3000
#            # 读取超时时间（毫秒）
#            readTimeout: 3000

feign:
  client:
    config:
      default:
        connect-timeout: 3000
        read-timeout: 3000

  httpclient:
    hc5:
      enabled: true # 开启 HttpClient5 支持
  sentinel:
    enabled: true # 开启 Feign 对 Sentinel 的支持

logging:
  level:
    # 格式：接口的全类名: DEBUG
    # 比如你的 Feign 接口在 com.abc.service 包下
    com.itguigu.cloud.api.PayFeignApi: debug

    io:
      seata: info

#seata:
#  enabled: true
#  application-id: ${spring.application.name}
#  tx-service-group: my_test_tx_group # 事务分组名，随便起
#  service:
#    vgroup-mapping:
#      my_test_tx_group: default # 映射到默认集群
#    grouplist:
#      default: localhost:8091 # Seata Server 的地址


#seata:
#  registry:
#    type: nacos
#    nacos:
#      server-addr: localhost:8848
#      namespace: ""
#      group: SEATA_GROUP
#      application: seata-server
#
#  tx-service-group: my_test_tx_group # 事务组，由它获得TC服务的集群名称
#  service:
#    vgroup-mapping: # 点击源码分析
#      my_test_tx_group: default # 事务组和TC服务集群的映射关系
#  data-source-proxy-mode: AT



# ======================== Seata 1.5.2 核心配置 ========================
seata:
  enabled: true
  application-id: ${spring.application.name}

  # 1. 事务分组名（关键点：就像是给你的事务起个频道号）
  tx-service-group: default_tx_group

  service:
    vgroup-mapping:
      # 这里的名字必须和上面 tx-service-group 的值一模一样
      # 映射的值 "default" 必须和 Seata-Server(Docker) 里的集群名一致
      default_tx_group: default

  registry:
    # 指定去哪里找 Seata 服务端
    type: nacos
    nacos:
      application: seata-server # 这个名字要和 Nacos 列表里出现的 Seata 名字对上
      server-addr: localhost:8848
      group: DEFAULT_GROUP
      namespace: "" # 留空代表使用 Nacos 的 public 命名空间

