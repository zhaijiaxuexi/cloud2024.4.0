# bootstrap.yml
spring:
  application:
    name: cloud-payment-service # prefix   prefix：默认为服务名（spring.application.name）
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      config:
        server-addr: localhost:8848 # Nacos地址
        file-extension: yaml # 后缀